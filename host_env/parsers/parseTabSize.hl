
;*
parser tabSize 100 (pi)
    var llist: last pi.clist
    return!
    if ((isExpr llist) && llist.1 == `tabSize)
        pi.clist.pop!
        pi.tabSize = (Number llist.2)
        true
    else
        false
;exports.parseTabSize = parseTabSize 
*;